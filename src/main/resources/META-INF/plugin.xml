<!-- Plugin Configuration File. https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!--
      IMPORTANT: this id is PERMANENT once the plugin is first uploaded to the
      JetBrains Marketplace. Confirm it before the first human upload.
    -->
    <id>fr.ascit.darkmoon</id>
    <name>Darkmoon</name>
    <vendor email="contact@asc-it.fr" url="https://darkmoon.asc-it.fr">ASC-IT</vendor>

    <description><![CDATA[
        <p><b>Darkmoon</b> brings your AI-driven penetration-test campaigns into the IDE.
        Browse campaigns and findings, read redaction-safe reports, and launch a new
        campaign — against both Darkmoon <b>OSS</b> (local CLI) and Darkmoon <b>Pro</b>
        (REST API), with Pro-only capabilities cleanly disabled on OSS.</p>
        <ul>
          <li>Campaigns and a sortable, filterable, searchable Vulnerabilities table.</li>
          <li>Finding detail: description, evidence (redacted by default) and remediation.</li>
          <li>Reports that are redaction-safe by default; the full rehydrated report is
              opened only on an explicit, local, two-key action.</li>
        </ul>
        <p>Secrets are stored in the IDE's PasswordSafe, never in plugin settings or logs.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.DarkmoonBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Darkmoon"
                    anchor="right"
                    factoryClass="fr.ascit.darkmoon.jetbrains.toolwindow.DarkmoonToolWindowFactory"/>

        <applicationService serviceImplementation="fr.ascit.darkmoon.jetbrains.settings.DarkmoonSettings"/>
        <applicationService serviceImplementation="fr.ascit.darkmoon.jetbrains.settings.DarkmoonSecretService"/>
        <projectService serviceImplementation="fr.ascit.darkmoon.jetbrains.services.DarkmoonProjectService"/>

        <applicationConfigurable
                parentId="tools"
                instance="fr.ascit.darkmoon.jetbrains.settings.DarkmoonSettingsConfigurable"
                id="fr.ascit.darkmoon.settings"
                displayName="Darkmoon"/>

        <notificationGroup id="Darkmoon" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
